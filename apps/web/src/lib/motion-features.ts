// Loaded on demand by LazyMotion (see providers.tsx), so the animation engine is not part of
// the JavaScript a page needs before it can be used.
export { domAnimation as default } from "motion/react";
