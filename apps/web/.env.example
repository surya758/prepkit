# Nothing to set for local development.
# In production, set this on the web host to the deployed API's address.
# To try another address locally, copy this file to .env.local (git-ignored).
API_URL=http://localhost:4000
