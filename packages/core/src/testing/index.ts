// Test helpers, published as "@prepkit/core/testing". Nothing here is imported by
// production code; it exists so this package's tests and the API's tests share one fake.
export * from "./fake-provider";
